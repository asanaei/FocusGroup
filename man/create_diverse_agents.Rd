% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convenience_wrappers.R
\name{create_diverse_agents}
\alias{create_diverse_agents}
\title{Create Diverse AI Agents for Focus Group}
\usage{
create_diverse_agents(
  n_participants,
  demographics = NULL,
  survey_responses = NULL,
  llm_config = NULL,
  runner = NULL,
  direct_persona_descriptions = NULL
)
}
\arguments{
\item{n_participants}{Integer number of participants to create}

\item{demographics}{Optional data frame with demographics. If NULL, generates diverse demographics.}

\item{survey_responses}{Optional data frame with survey responses. If NULL, generates responses.}

\item{llm_config}{List with LLM configuration parameters}

\item{runner}{`NULL` or a function `(config, messages)` returning a character
scalar or an `llmr_response`. The function is stored on every agent.}

\item{direct_persona_descriptions}{Optional character vector of pre-rendered
participant personas. When supplied, these descriptions are used directly
and are recycled in order if necessary. Demographics and survey responses
remain attached as raw reporting fields.}
}
\value{
A named list of FGAgent objects (participants + 1 moderator), keyed
  by agent ID.
}
\description{
Creates a diverse set of AI agents with varied demographics and personas
for use in focus group simulations.
}
\examples{
\dontrun{
# Create 6 diverse participants
agents <- create_diverse_agents(6)

# Create with custom demographics
demo_data <- data.frame(
  age = c(22, 35, 28, 41, 19, 33),
  gender = c("Female", "Male", "Male", "Female", "Male", "Female"),
  education = c("Bachelor's", "Master's", "High School", "PhD", "Some College", "Bachelor's")
)
agents <- create_diverse_agents(6, demographics = demo_data)
}

}
